[includeIf "gitdir:~/Code/Github/"]
  	path = .gitconfig-github

[includeIf "gitdir:~/.local/share/chezmoi/"]
  	path = .gitconfig-github

[includeIf "gitdir:~/Code/Gitlab/"]
  	path = .gitconfig-gitlab

[includeIf "gitdir:~/Code/Gitea/"]
  	path = .gitconfig-gitea


[alias]
	i = init
	s = status
	a  = add
	ac = commit -am
	cl = clone
	co = checkout
	pl = pull
	ph = push
    so = remote set-url origin
	cob = checkout -b

[core]
	eol = lf
	autocrlf = input

[init]
	defaultBranch = main

[web]
	browser = firefox

[program]
  pgp = /usr/bin/gpg
